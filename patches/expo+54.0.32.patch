diff --git a/node_modules/expo/src/async-require/hmr.ts b/node_modules/expo/src/async-require/hmr.ts
index 95b8044..45578fe 100644
--- a/node_modules/expo/src/async-require/hmr.ts
+++ b/node_modules/expo/src/async-require/hmr.ts
@@ -65,6 +65,21 @@ function assert(foo: any, msg: string): asserts foo {
   if (!foo) throw new Error(msg);
 }
 
+function getBundleUrlFromScripts(): string | null {
+  if (typeof document === 'undefined') {
+    return null;
+  }
+  const scripts = document.getElementsByTagName('script');
+  for (let i = scripts.length - 1; i >= 0; i -= 1) {
+    const src = scripts[i]?.src;
+    if (!src) continue;
+    if (src.includes('.bundle') || src.includes('platform=')) {
+      return src;
+    }
+  }
+  return null;
+}
+
 /**
  * HMR Client that receives from the server HMR updates and propagates them
  * runtime to reflects those changes.
@@ -163,10 +178,11 @@ const HMRClient: HMRClientNativeInterface = {
 
     const fullBundleUrl = (() => {
       const currentScript = document?.currentScript;
-      const bundleUrl = new URL(
-        currentScript && 'src' in currentScript ? currentScript.src : location.href,
-        location.href
-      );
+      const scriptSrc =
+        (currentScript && 'src' in currentScript && currentScript.src) ||
+        getBundleUrlFromScripts() ||
+        location.href;
+      const bundleUrl = new URL(scriptSrc, location.href);
 
       if (!bundleUrl.searchParams.has('platform')) {
         bundleUrl.searchParams.set('platform', process.env.EXPO_OS ?? 'web');
